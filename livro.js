const button=document.querySelector('#start-reading'),message=document.querySelector('#action-message'),key='loner-evangelho-reading';function paint(){const saved=localStorage.getItem(key)==='true';button.textContent=saved?'Marcado para ler':'Marcar para ler';button.setAttribute('aria-pressed',String(saved))}button.addEventListener('click',()=>{const saved=localStorage.getItem(key)!=='true';localStorage.setItem(key,String(saved));paint();message.textContent=saved?'Obra adicionada às suas leituras.':'Obra removida das suas leituras.';setTimeout(()=>message.textContent='',2200)});paint();
